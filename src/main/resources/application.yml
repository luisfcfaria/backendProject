server:
  port: ${SERVER_PORT:8080}

spring:

  output:
    ansi:
      enabled: ALWAYS
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDB103Dialect
    hibernate:
      ddl-auto: ${HIBERNATE_DDL_AUTO:create-drop}
      show-sql: true
  datasource:
      url: ${MARIADB_URL:jdbc:mariadb://j5zntocs2dn6c3fj.chr7pe7iynqr.eu-west-1.rds.amazonaws.com:3306/v18agywqp0019vom}
      username: ${MARIADB_USER:yigv7bkgk4erdf4b}
      password: ${MARIADB_PASSWORD:xf38cj9blzbyvn8x}
jwt:
  token:
    validity: 18000
    prefix: Bearer
  signing:
    key: signingkey
  authorities:
    key: roles
  header:
    string: Authorization
mailing:
  sendgrid:
    api-key: ${SENDGRID_API_KEY}
    enabled: true

